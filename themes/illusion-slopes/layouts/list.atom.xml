{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
{{/* Atom feed to replace the RSS 2.0 feed generated by Hugo by default. Note
  the content type definitions added in the theme's hugo.toml, too.

  By default, even when using this theme, you will only get RSS feeds; to enable
  Atom as an output format, you have to specify so in your *site*'s hugo.toml.
*/}}

{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = .Site }}{{ end }}
{{- $pages := slice }}
{{- if or $.IsHome $.IsSection }}
  {{- $pages = $pctx.RegularPages.ByDate.Reverse }}
{{- else }}
  {{- $pages = $pctx.Pages.ByDate.Reverse }}
{{- end }}


<feed xmlns="http://www.w3.org/2005/Atom">
  <title type="text">
    {{ if or .IsHome (not .Title) }}
      {{ .Site.Title }}
    {{ else }}
      {{ printf "%s | %s" .Title .Site.Title }}
    {{ end }}
  </title>

  {{ with .OutputFormats.Get "atom" }}
    <link href="{{ .Permalink }}" rel="self" />
  {{ end }}
  <link href="{{ .Site.BaseURL }}" rel="alternate" />

  {{ with or .Site.Params.subtitle .Site.Params.description }}
    <subtitle type="text">{{ . }}</subtitle>
  {{ end }}

  {{ with (collections.Index $pages.ByLastmod.Reverse 0) }}
    <updated>{{ time.Format "2006-01-02T15:04:05-07:00" .Lastmod }}</updated>
  {{ end }}


  <id>{{ .Site.BaseURL }}</id>

  {{/* TODO: continue applying template */}}
  <author>
    <name>Max Kapur</name>
    <email>max@maxkapur.com</email>
  </author>
  <rights type="text">Creative Commons CC BY-SA 4.0 license</rights>

  <entry>
    <title type="text">PageRanking People and Blogs interviews</title>
    <link
      rel="alternate"
      href="https://maxkapur.com/2026/05/06/pageranking-pb.html"
    />

    <!--
      Use post.date as both the updated and published date. Currently, we don't
      keep separate track of when a post was updated.
    -->
    <updated>2026-05-06T00:00:00-05:00</updated>
    <published>2026-05-06T00:00:00-05:00</published>

    <!--
      An RFC 4287 Simple Extension element: human-readable representation of the
      publication date. We use this in the stylesheet to show a concise date
      instead of the dense ISO format string.
    -->
    <readableDate>May 6, 2026</readableDate>

    <id>https://maxkapur.com/2026/05/06/pageranking-pb</id>
    <summary type="html">Summary example</summary>
    <content type="html">Content example</content>
  </entry>
</feed>
