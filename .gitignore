_site
.sass-cache
.jekyll-cache
.jekyll-metadata

# bundle install local targets
vendor

# From micro editor
log.txt

# Larger versions of favicons, images
*-large.jpg

# `bundle install` produces a slightly different lockfile on different Linux
# distributions
Gemfile.lock

# conda environment prefix
.conda
